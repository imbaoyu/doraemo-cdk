{
  "name": "lambda",
  "version": "1.0.0",
  "description": "code of aws lambda functions",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1599.0"
  }
}
